# Inputs

Put your personal Advent of Code inputs here as untracked files named `dayNN.txt`.

Examples:
- `inputs/day01.txt`
- `inputs/day17.txt`

These files are intentionally gitignored to avoid redistributing puzzle inputs.
Tests that need one skip when it is missing, so a fresh clone stays green.
